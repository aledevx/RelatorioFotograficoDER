@model IEnumerable<RelatorioFotograficoDER.Models.ProcessoManutencao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RelatoriofotograficoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoordenacaoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RelatorioAtesteId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RelatoriofotograficoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoordenacaoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RelatorioAtesteId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
